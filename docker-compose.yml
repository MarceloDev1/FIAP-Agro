version: "3.9"

services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"

  alert-api:
    build:
      context: .
      dockerfile: FIAP.Agro.Alert/Dockerfile
    ports:
      - "8081:8080"
    depends_on:
      - rabbitmq